# Copy to .env.local (never commit .env.local). Loaded after the environment; if a key is
# already in Claude/Hypernative/.env.local you do not need to repeat it here.
SYNCRONE_API_KEY=
ETHERSCAN_API_KEY=
SAFE_API_KEY=
VAULTS_FYI_API_KEY=
COINGECKO_API_KEY=   # optional demo key: batches token pricing for clients without Syncrone
